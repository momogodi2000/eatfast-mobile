<?xml version="1.0" encoding="utf-8"?>
<full-backup-content>
    <!-- Include user preferences only -->
    <include domain="sharedpref" path="."/>

    <!-- Exclude sensitive authentication data -->
    <exclude domain="sharedpref" path="FlutterSecureStorage"/>
</full-backup-content>